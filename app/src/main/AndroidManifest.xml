<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/customer_service"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BCASPAY"
        tools:targetApi="31">
        <activity
            android:name=".splashScreen.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity android:name=".LoginActivity"/>-->
        <activity android:name=".view.home.HomeMainActivity"/>
        <activity android:name=".view.register.RegisterActivity"/>
        <activity android:name=".view.biodata.BiodataActivity"/>
        <activity android:name=".view.detil.DetilNewsActivity"/>
        <activity android:name=".view.detil.DetilNewsListActivity"/>
        <activity android:name=".view.login.LoginActivity"/>
    </application>

</manifest>